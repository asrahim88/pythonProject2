#Theory of Triangle area = 1/2 * base * height

base = float(input("Enter base number: "))
height = float(input("Enter height number: "))
triangle = .5 * base * height
print("Area of triangle: = ", triangle)